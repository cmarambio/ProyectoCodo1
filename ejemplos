<!DOCTYPE html>
<html>
<head>
  <style>
    /* Estilo para la lista desplegable */
    .dropdown {
      position: relative;
      display: inline-block;
    }

    /* Estilo para el botón que activará la lista desplegable */
    .dropdown-button {
      cursor: pointer;
      border: 1px solid #ccc;
      padding: 10px;
    }

    /* Estilo para la lista desplegable oculta por defecto */
    .dropdown-content {
      display: none;
      position: absolute;
      background-color: #f9f9f9;
      min-width: 160px;
      box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
      z-index: 1;
    }

    /* Estilo para los elementos de la lista desplegable */
    .dropdown-content a {
      padding: 12px 16px;
      text-decoration: none;
      display: block;
    }

    /* Estilo para los elementos de la lista desplegable al pasar el puntero sobre ellos */
    .dropdown-content a:hover {
      background-color: #ddd;
    }

    /* Muestra la lista desplegable cuando se pasa el puntero sobre el botón */
    .dropdown:hover .dropdown-content {
      display: block;
    }
  </style>
</head>
<body>

<div class="dropdown">
  <div class="dropdown-button">Pasa el puntero aquí</div>
  <div class="dropdown-content">
    <a href="#">Opción 1</a>
    <a href="#">Opción 2</a>
    <a href="#">Opción 3</a>
  </div>
</div>

</body>
</html>
